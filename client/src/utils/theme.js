import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    body: "Nunito",
    heading: "Nunito",
  },
  colors: {
    primary: "#283C62",
    secondary: "#5c6f88",
    grayText: "#c8c8d1",
  },
});

export default theme;
